#!/usr/bin/env node
import 'source-map-support/register';
import * as cdk from 'aws-cdk-lib';
import { WorkingDaysApiStack } from '../lib/working-days-api-stack-ts';

const app = new cdk.App();
new WorkingDaysApiStack(app, 'WorkingDaysApiStack', {});
